version: '2'
services:
  web:
    build:
      context: .
      dockerfile: ./docker/rails/Dockerfile.production
    command: bundle exec rails s -p 80 -b '0.0.0.0' -e production
    volumes:
      - .:/api
    ports:
      - 80:80
    extends:
      file: ./docker/mysql/db_config_production.yml
      service: settings
